# Backend Configuration
# For local development, this should point to your local backend
# For production/Vercel deployment, this should point to your deployed backend URL
VITE_BACKEND_URL=http://localhost:3000

# API Base URL for frontend requests
# In development, this is typically /api which gets proxied
# In production, this should be the full backend URL
VITE_API_BASE_URL=/api

# Environment
NODE_ENV=development

# For Production Deployment on Vercel:
# Set these environment variables in your Vercel project dashboard:
# VITE_BACKEND_URL=https://backend-split-smart.onrender.com
# VITE_API_BASE_URL=https://backend-split-smart.onrender.com
# NODE_ENV=production